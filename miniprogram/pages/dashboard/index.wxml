<view class="page"><text class="h1">家庭数据看板</text><text class="subtitle">截至 {{stats.today}} · 全部历史记录</text><view class="grid mt"><view wx:for="{{cards}}" wx:key="label" class="card"><text class="hero-number">{{item.value}}</text><text class="subtitle">{{item.label}}</text></view></view><text class="h2 mt">最常点的菜 · 前 10</text><view wx:for="{{stats.popular}}" wx:key="dishId" class="card mt-sm row between"><text>{{index+1}}. {{item.dishName}}</text><text>{{item.count}} 次</text></view><text class="h2 mt">掌勺贡献 · 前 10</text><view wx:for="{{stats.cooks}}" wx:key="memberId" class="card mt-sm row between"><text>{{item.memberName}}</text><text>完成 {{item.count}} 道</text></view><text class="h2 mt">最近 10 个有点菜的日期</text><view wx:for="{{stats.trend}}" wx:key="date" class="card mt-sm"><view class="row between"><text>{{item.date}}</text><text>{{item.completed}} / {{item.count}} 完成</text></view><view class="progress"><view class="progress-bar" style="width:{{item.completed / item.count * 100}}%"></view></view></view><view class="note mt">取消事项不计入热门菜与完成进度。评价按成员、菜品、菜单日期去重；没有评价不计为 0 分。</view></view>