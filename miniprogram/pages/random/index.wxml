<view class="page"><text class="h1">今天吃什么？</text><text class="subtitle">随机挑选当前可点的菜，每次不重复；可以多次换一组。</text><view class="field"><text class="field-label">想吃几道菜（1—50）</text><input class="input" type="number" value="{{count}}" bindinput="countInput" /></view><button class="btn-primary" bindtap="refresh" disabled="{{loading}}" loading="{{loading}}">{{items.length ? '换一组' : '随机选菜'}}</button><view class="stack mt"><view wx:for="{{items}}" wx:key="id" class="card"><text class="h3">{{index+1}}. {{item.name}}</text><photo-gallery wx:if="{{item.coverMediaId}}" ids="{{[item.coverMediaId]}}" /><text class="subtitle">{{item.introduction}}</text></view></view><button wx:if="{{items.length}}" class="btn-primary mt" disabled="{{loading}}" bindtap="confirm">就吃这些 · 加入已选 {{items.length}} 道</button><view class="note mt">确认后进入已选菜品，可分别填写备注。提交后等待家人认领、做菜、完成和评价。</view></view>