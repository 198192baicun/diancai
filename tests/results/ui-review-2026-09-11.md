# 界面与代码检查记录

## 修改范围

原生小程序：今日菜单、我的点菜使用状态下拉选择器；首页文字操作靠右；我的入口和历史卡片占满内容宽度；分类使用两行卡片。菜单、我的点菜、历史及分类计数读取完整分页；菜单和我的点菜拒绝迟到的旧筛选响应。分页读取期间成员或家庭上下文变化时拒绝返回拼接结果。

## 审查结论

- 已修复：分类数量只统计前 50 道菜；菜单、我的点菜、历史只展示第一页；快速切换状态可能被旧响应覆盖。
- 已检查服务端 family/catalog/menu/vote、数据库初始化和 HTTP/媒体关键路径；现有 HTTP 集成检查覆盖批次事务、回执重放、状态权限、每日评价、投票一次加入、历史及原文件流程，未在这些已检查场景中发现新增失败。此结论不等于所有输入和设备环境均已验证。
- 其余列表页面仍需评估分页：本次完整分页修复仅覆盖上述四个页面。
- 文档文件存在命名异常；README 指向的产品规则实际以乱码文件名保存，配置决策及系统设计文件未找到。遵循已有产品规则、接口及用户提供的仓库约束，未自行补造缺失决定。
- 生产镜像、运行库门槛、停服维护与真机发布证据仍不完整；本次没有执行部署或操作家庭数据。

## 实测

- `npm test`：16 项通过，包括 3 项客户端运行逻辑回归检查（模拟请求及 Page 环境，并非微信运行）。
- `npm run typecheck`：通过。
- `node --test tests/domain.test.cjs`：29 项通过，属于原型领域检查。
- `python tests/schema_test.py`：25 项通过，使用临时 SQLite。
- `python tools/build_prototype.py`：通过；原型构建不能验证原生 WXML 布局。

## 未通过或未执行

- `python tests/deployment_test.py`：缺少 PyYAML，未执行检查。
- `python tests/browser_test.py`：缺少 Playwright，未执行检查。
- `python tools/build_previews.py`：缺少 Pillow，未生成预览。
- 微信开发者工具与 iOS/Android 的视觉、触控和网络实测：未执行。截图用于定位问题，不能作为修改后的验收证据。
