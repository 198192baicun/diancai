# 原生小程序修复与功能检查

## 当前交付

- 稳定签名游标和服务端总数；长列表可继续加载，搜索与筛选拒绝旧响应覆盖。
- 投票允许多选，勾选后统一保存；过期的获胜菜选择由服务端拒绝。
- multipart 任意网络分块保留原字节；步骤图片按固定步骤标识回填。
- 评价恢复前台保留输入；批次代次变化提示；成功回执清理时先清草稿再清凭据。
- 菜谱与事项先展示文字，图片随后加载且可点开大图；编辑冲突逐项对照选择。
- 我的待做、按家庭与成员隔离的本机菜谱编辑草稿。
- 图文导入入口：小红书公开页面解析；保存图片与原文导入；先生成草稿，核对后保存公共菜品。
- 产品、交互、API 与验收文档同步。产品、交互、验收文档文件名已恢复为其引用使用的中文名称；缺失的系统设计和配置决策文档未补造。

## 检查结果

环境：Windows、Node.js v24.16.0、Python 3.12.10。测试数据库和媒体写入临时目录；未操作真实家庭数据库、停服、恢复或部署。

| 命令 | 结果 | 范围 |
|---|---|---|
| `npm test` | 29项通过 | HTTP、SQLite开发服务、原生客户端静态及模拟运行回归 |
| `npm run typecheck` | 通过 | TypeScript静态类型 |
| `node --test tests/domain.test.cjs` | 29项通过 | 原型领域模型 |
| `python tests/schema_test.py` | 25项通过 | 临时SQLite结构与事务 |
| `python tools/build_prototype.py` | 通过 | 原型构建，非新增原生页面渲染 |
| `python tests/deployment_test.py` | 未执行检查 | 缺少PyYAML |
| `python tests/browser_test.py` | 未执行检查 | 缺少Playwright |
| `python tools/build_previews.py` | 未生成 | 系统Python缺少Pillow；改用自带Python后缺少fc-match |

检查期间首次 `npm test` 有4项未通过：两个旧页面测试的依赖替身不认识新的分页读取器、页面数量仍断言24、投票仍断言即时提交时的选择表达式。测试已按27页和“勾选后保存”的确定行为同步；原有行为断言保留并补充针对性回归后复测通过。

## 分享链接验证

- 小红书示例：浏览器可展示正文和10张配图；实际公开HTML经当前解析器读取成功，正文791个码点，含做法正文，提取10个配图地址。只验证读取与解析，没有把这篇教程写进真实家庭库。
- 受控响应导入检查：完整图片原字节落入临时媒体目录，返回顺序与正文一致，未创建菜品；图片下载失败不返回成功草稿。
- 服务端公开链接完整导入：当前DNS将xhslink.cn解析到198.18地址段，地址保护拒绝，未取得真实网络全流程通过证据。没有放宽地址保护。
- 抖音示例：浏览器可显示图文页及4张图片；服务端普通请求返回浏览器脚本页面，没有可解析正文和图片数据。自动导入未实现可用结果；提供用户保存图片后的导入入口。

样例：[小红书](https://xhslink.cn/o/AQkLjy02F52)、[抖音](https://v.douyin.com/LpasytxF5Qs/)。平台公开页面可随时改变；来源图片可能已经被平台处理，不承诺作者上传前的原始文件。

## 尚未验收

微信开发者工具实际编译、iOS/Android真机触控、前后台与存储限制、目标Linux网络下的公开链接完整导入、生产镜像和运维任务均未执行。本次未增加生产运行依赖或数据库迁移。

## 导入按钮反馈补充检查

微信开发者工具中有链接但点击无响应的问题尚未现场复现。客户端现提供读取中状态、失败弹窗、空输入提示和草稿保存后跳转失败提示；上下文读取异常纳入错误处理。新增测试验证有链接点击发送请求、重复点击保护、失败后恢复及跳转失败反馈。

验证：npm test，32 项通过；npm run typecheck，通过。测试使用微信接口替身，不代表开发者工具或平台真实导入成功。仍需重新编译后取得实际请求结果。
