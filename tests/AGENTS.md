# 检查与验收

遵守根目录约束。负责可复现证据与覆盖边界，不把演示当生产。

`domain.test.cjs` 检查浏览器领域模型；`schema_test.py` 检查临时 SQLite 的 DDL/事务；`browser_test.py` 检查原型渲染与交互；`server.test.cjs` 检查真实 HTTP/SQLite 开发实现；`miniprogram-static.test.cjs` 检查原生小程序工程结构和关键客户端契约。各层次分开统计，浏览器 22 视图 × 3 宽度属于一项布局检查，不重复扩大测试数。

浏览器执行环境采用 HTML 文本注入与仅限测试的存储替身，不能改变管理策略。新文档重新初始化不是原生 file:// 持久化、真实浏览器重启或微信存储验收。所有证据保存实际命令、运行库版本、失败项与未覆盖范围。

测试必须使用临时数据，不能读取真实家庭数据库或删除真实照片。不要移除失败用例、吞掉页面异常或强行修改阈值来满足通过数。产品决定改变时先写清规则，再同步断言。

当前已有 HTTP API/SQLite 开发实现检查；后续继续补充生产驱动、linux/amd64 镜像、运维和微信真机证据，保留来源层次。生产运行库门槛与当前临时测试环境不同；DDL 或开发服务用例通过不意味着旧运行库可进入正式镜像。

运行 tests/deployment_test.py 校验 linux/amd64、Asia/Shanghai 与备份策略；这是静态配置及宿主校验工具检查，不是 Docker 部署或维护任务验收。CPU、时区和备份已确认，不重新列为待选项。
