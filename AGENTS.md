# 项目协作约束

## 适用范围与阅读顺序

本文件适用于整个仓库；子目录 `AGENTS.md` 补充本目录职责，不能放宽全局业务与数据安全规则。先读 `README.md`、`docs/01_产品规则.md` 和负责目录的说明，再开始工作。

权威顺序：`docs/06_配置与决策.md` 的明确决定 → 产品规则 → `contracts/http-api.md` 的接口语义 → 系统/交互/运维设计。DDL 是数据库结构的可执行来源；原型负责交互表达，不是绕过服务权限的依据。遇到来源冲突先提交具体冲突点，不自行创造新的业务规则。

## 当前边界

单家庭、可信家庭局域网 Linux、Docker Compose 单应用容器、单进程、SQLite 本机文件与原文件媒体。原生微信客户端；不建设 Windows 服务交付、网页运维台、线上数据库热切换、Redis、消息队列、微服务或多租户。

成员选择只表达业务归属，不是认证。禁止暴露公网，禁止把成员 ID 当安全令牌。已确认 `linux/amd64`、`Asia/Shanghai`、每日北京时间 04:00 停服备份、最近 10 份成功包和每周异介质副本。不再重复询问这三项，不建设其他服务端平台发行路线。现场 IP、镜像 digest、路径和设备信息仍需真实记录。

## 不可破坏的规则

- 重复菜品是独立点菜事项；整批提交原子化，成功回执与事项同事务提交。结果未知时保持原 Key 与载荷，不能以查无回执为由重新点菜。
- 状态与操作者在服务事务内检查；历史快照不可被公共资料编辑覆盖。跨天未完成保留原日期，菜品不上物理删除路径。
- 成员停用检查全日期依赖；投票单活跃、每成员可多选且同一候选最多一票、同轮最多加入菜单一次；取消条目不参与有效进度和评分。
- 文件上限 52,428,800 字节，原字节保存。识别、预览与下载分开；所有引用受保护，清理只在排他停服维护中执行。
- 恢复同时处理数据库与被引用媒体，并产生新数据代次；不自动重送旧客户端意图。部署及运行库必须满足技术门槛。

## 工作方式

按业务模块和必要职责拆文件，不为简单功能套固定多层模板。修改行为时同步产品规则、接口、DDL/模型、交互与对应测试；不要让各文件各自描述不同规则。目录所有者负责该目录主要变更，跨目录契约先约定后提交，避免覆盖其他协作者的未完成内容。

先检查真实文件和工具，不虚构 package.json、镜像、命令或构建成功。本仓库包含原生微信小程序工程与可本地运行的 HTTP/SQLite 开发实现；生产镜像、维护命令和真机发布验收仍须以实际产物与证据为准。

检查命令在项目根目录：

```sh
node --test tests/domain.test.cjs
python3 tests/schema_test.py
python3 tests/deployment_test.py
python3 tools/build_prototype.py
python3 tests/browser_test.py
python3 tools/build_previews.py
```

新增依赖先说明必要性并锁定版本；公开技术事实以官方资料为准。不要读取生产秘密、在命令行输出秘密或访问不属于当前任务的数据。涉及真实数据清理、恢复、发布、覆盖或停服必须取得明确执行授权，不以设计讨论代替授权。

## 交付标准

交付文档只描述当前产品，不包含过程评语、历史对比或未决方案混写。演示、模拟、实测和未执行须明确区分。失败检查必须如实记录，不能删除测试或改宽阈值以制造通过。验收边界见 `docs/05_开发与验收.md`。
