# 复制为 .env，填写真实镜像、LAN 地址和已经初始化的数据根目录。
# 平台 linux/amd64 与业务时区 Asia/Shanghai 已固定在 compose.yaml，不通过此文件选择。
# 空必填值使 Compose 拒绝启动；本生产模板没有已验收的配套镜像或初始化维护命令。
# 开发容器使用 compose.dev.yaml 和 dev.env.example，不使用本文件。
APP_IMAGE=
HOST_LAN_IP=
HOST_PORT=8080
DATA_ROOT=
APP_UID=10001
APP_GID=10001
