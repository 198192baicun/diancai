# 设计文档

遵守根目录约束。文档描述当前产品，避免过程叙事、重复方案或未确认决定混写。

产品规则定义权限与状态，系统设计定义结构与事务，接口契约定义请求语义，运维定义数据安全流程，验收文档区分已有材料与真实实现。跨文档修改要核对同一字段、状态、大小单位、时区和成功条件。

已确认 11 项选择写在 `06_配置与决策.md`。CPU、业务时区和备份策略不是未决项；现场信息通过部署记录填写，不要求用户重复选择。确有无法从现有方案推出的产品决定时才列简短选项。

技术事实引用官方依据并标检索时间。页面无法访问时明确限制，不从第三方摘要推导平台支持结论。测试数只取真实结果；不要将未运行环境标为通过。

链接只指向实际存在的文件。示例值标为示例，镜像、AppID 和 IP 不伪造现场值；平台、时区和备份方案与当前决策表一致。
