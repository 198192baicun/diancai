# 安全说明

本项目仅适用于单家庭、可信家庭局域网。成员选择表达业务归属，**不是身份认证**；能够访问服务的人可能冒用成员。不得暴露公网，不得把成员 ID 当安全令牌。

当前仓库提供开发 HTTP/SQLite 实现；生产镜像、维护命令及微信正式发布尚需实机验收。检查通过不代表已经达到生产交付要求，详见 [开发与验收](docs/05_开发与验收.md)。

## 报告漏洞

若仓库启用了 GitHub 私密漏洞报告，请从仓库 Security 页面使用 Report a vulnerability。若该入口不存在，请通过维护者在 GitHub 个人资料中公开提供的私密联系方式联系；没有可用渠道时，只在 Issue 请求建立私密联系，不公开漏洞细节。

报告请提供受影响版本或提交、使用虚构数据的复现步骤、预期与实际结果及影响范围。不要上传真实数据库、家庭照片、录音、访问凭据或完整私人日志。项目没有承诺固定响应时限。

## 数据与接入

Host 白名单默认仅允许本机地址与监听端口，LAN 地址须明确配置；开发 Compose 使用绑定 IP 与发布端口。Origin 默认拒绝，确需浏览器联调时逐项允许完整来源；无 Origin 的原生请求可用。仅本机健康 GET 有回环 Host 例外。域名和来源检查不替代网络隔离或认证。媒体读取故障及取消下载只结束该次传输，不应导致应用进程退出。

数据库与媒体保存在本地，原媒体不会自动移除元数据。备份必须同时覆盖数据库与被引用媒体；恢复会产生新数据代次，旧客户端意图不得自动重送。维护操作仅在授权的排他停服流程中执行。

`.gitignore` 不能清除已经提交的内容。发布前应检查暂存区和 Git 历史；若凭据曾被提交，应先撤销或轮换，再处理历史，不能仅删除当前文件。
